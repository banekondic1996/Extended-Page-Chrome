# Extended-Page-Chrome
New tab page extension that complements Extended History

**To do:** </br>
<pre>
-Add option to view active tabs in sidebar
-Add timer widget
-Add notes widget
-Add option to put weather under clock (icon + temp, setting city in settings)
-Add optional option for greeting message
-Add option for high res wallpapers feed
-Add option to disable search input
-Add currency conversion widget option
-Option to change "wallpaper-overlay" opacity from 0.5 to 1 range (keep text color var(--text)
 for white color for 'site-name' and '#clock-date' what ever uses --text2)
</pre>
